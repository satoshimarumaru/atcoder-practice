import sys
input = sys.stdin.readline


def main():
    # 1行で1つの整数を読み込む場合
    # n = int(input())

    # 1行でスペース区切りの整数を読み込む場合
    # a, b = map(int, input().split())

    # n行で整数を読み込む場合
    # a = [int(input()) for _ in range(n)]

    pass


if __name__ == "__main__":
    main()
